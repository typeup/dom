name: Bump Beta

on:
  workflow_dispatch:
  push:
    branches: ["master-beta"]

jobs:
  bump-beta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Bump beta (placeholder)
        run: echo "Running bump-beta for ${GITHUB_REF}"
