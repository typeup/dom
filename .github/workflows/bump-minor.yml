name: Bump Minor

on:
  workflow_dispatch:
  push:
    branches: ["master"]

jobs:
  bump-minor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Bump minor (placeholder)
        run: echo "Running bump-minor for ${GITHUB_REF}"
