name: Bump Common

on:
  workflow_dispatch:
  push:
    branches: ["master", "master-2"]

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Bump common (placeholder)
        run: echo "Running bump-common for ${GITHUB_REF}"
