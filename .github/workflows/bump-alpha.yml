name: Bump Alpha

on:
  workflow_dispatch:
  push:
    branches: ["master-2"]

jobs:
  bump-alpha:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Bump alpha (placeholder)
        run: echo "Running bump-alpha for ${GITHUB_REF}"
